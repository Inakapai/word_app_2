$(function(){
    $('.frame').html('<%= j(render 'question_another',number: $count_number, question: @question_next, similars: @similars_next) %>');
    
    if (<%= $count_number %> == 10 ){
        $('.frame').html('<%= j(render 'question_last',number: $count_number, question: @question_next, similars: @similars_next) %>');
    }
});